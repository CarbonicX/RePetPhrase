# RePetPhrase
Re:PetPhrase 自动修改玩家和替换发送的消息

需要前置模组 `Mod Menu` 和 `Cloth Config API`

---

- 忽略标记：以该标记开头的消息将不会被修改
- 消息前缀、消息后缀、短句前缀、短句后缀支持多个，将会被随机选择，使用英文分号隔开；文本内使用分号需用两个分号

例：`a;;b;c` -> `a;b` 和 `c`

- 替换：替换消息中出现的文本。`待替换文本;新文本1;新文本2;...`
- 惯用语：将与文本完全符合的消息直接替换为惯用语。`消息;惯用语1;惯用语2;...`
